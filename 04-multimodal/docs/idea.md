# 📊 Персональный финансовый советник — Telegram Bot

Персональный финансовый советник — это Telegram-бот, который автоматически ведёт учёт доходов и расходов пользователя, анализируя текст, изображения и голосовые сообщения. Бот построен на базе LLM/VLM-моделей и обеспечивает полуавтоматический или полностью автоматический финансовый трекинг.

---

## 🚀 Основная идея

Бот помогает пользователю фиксировать финансовые транзакции прямо в чате. Он понимает сообщения естественным языком, распознаёт текст с изображений (чеки, выписки), а также транскрибирует голосовые сообщения. На основе извлечённых данных бот формирует структуру расходов и доходов и предоставляет отчёты.

---

## ✨ Ключевые возможности

### 📘 Основной функционал

* Ведение структурированной таблицы учёта доходов и расходов.
* Автоматическое извлечение данных о транзакциях из текстовых сообщений.
* Формирование отчётов:

  * текущий баланс
  * суммарные доходы/расходы
  * отчёты за период
* Поддержка нескольких режимов работы.

### 🖼 Обработка изображений

* Анализ чеков, скриншотов банковских операций и других документов.
* Извлечение структурированных данных из изображений с помощью **`qwen/qwen2.5-vl-32b-instruct`**.

### 🎤 Работа с голосовыми сообщениями

* Распознавание речи из аудио через **Whisper**.
* Автоматическое извлечение транзакций из расшифрованного текста.

### 🤖 Используемые модели

* LLM со structured output для:

  * анализа текстовых сообщений
  * выявления транзакций
  * формирования ответов
* VLM (**Qwen2.5-VL-32B-Instruct**) для обработки изображений
* Whisper (speech-to-text) для обработки аудиосообщений

---

## 🧩 Технологический стек

* **Telegram Bot API**
* **LLM / VLM модели**
* **Whisper STT**
* **Поддержка локальных и внешних моделей**
* Python (aiogram/pyTelegramBotAPI), FastAPI или любой другой бекенд по необходимости

---

## 🏗 Архитектура (кратко)

```
Пользователь → Telegram Bot  
  → (Текст) → LLM → Extracted Transaction  
  → (Фото) → Qwen2.5-VL → Extracted Transaction  
  → (Голос) → Whisper → LLM → Extracted Transaction  
      ↓
  Таблица учёта (SQLite / PostgreSQL / Google Sheets / др.)
      ↓
  Отчёты и аналитика → Пользователь
```

---

## 📦 Возможные режимы

* **Простой ответ**
  Бот отвечает как обычный ассистент.

* **Использовать коллекцию**
  Обработка накопленных данных/транзакций.

* **Добавить коллекцию (файл)**
  Импорт данных из Excel/CSV.

